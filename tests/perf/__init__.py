"""Performance fixtures and benchmarks.

A package so the generated benchmark lexicon has one importable home shared by
the generator's own determinism test and, later, the perf gate that measures
against it.
"""
